node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local-only sales context brought from syphon-sales (rules, plan, KB).
# NEVER commit to the public Cyprus repo (contains internal strategy).
.cursor/
